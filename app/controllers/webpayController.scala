package controllers

import play.api.mvc._

import module.webpay.{ WechatPayModule }
import controllers.common.requestArgsQuery.{requestArgs}

object webpayController {
//	  def wechatPrepayID = Action (request => requestArgs(request)(WechatPayModule.prepayid))
}